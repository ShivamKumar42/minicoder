# MiniCoder (package)

Full documentation lives in the repository root: [`../README.md`](../README.md).

Quick links:

- Source: `minicoder/` (agent loop in `agent.py`, tools in `tools/`, providers in `llm/`)
- Tests: `minicoder/tests/` — run `python -m pytest minicoder/tests/ -v` from the repo root (145 tests, no API keys required)
- Example config: `../.env.example`
- Demo target for manual runs: `demo_project/`
